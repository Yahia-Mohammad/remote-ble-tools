interface:
  display_name: "RemoteBLE"
  short_description: "Configure and safely operate RemoteBLE devices"
  default_prompt: "Use $remoteble to configure the CLI and safely inspect a RemoteBLE-connected device."
policy:
  allow_implicit_invocation: true
