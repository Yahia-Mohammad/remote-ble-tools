# Changelog

This project follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and uses semantic
versioning. Entries are finalized only when their release tag is published.

## [Unreleased]

### Added

- Packaged JVM CLI process, PTY, broken-pipe, lifecycle, schema, and local-state coverage.
- Matching-host JVM↔Native state-lock and extracted-archive smoke checks in CI.
- A manual desktop-agent hardware validation record for the v0.1 release gate.

## [0.1.0] - Pending release

Initial RemoteBLE Tools release: macOS arm64 and Linux x64/arm64 Native distributions, a JVM
compatibility archive, versioned JSON and JSONL contracts, policy-controlled GATT writes, bounded
streams, persistent sessions, local audit logging, and the portable RemoteBLE Agent Skill.
